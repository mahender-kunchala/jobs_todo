from flask import render_template,request,redirect,url_for,Blueprint,g
from . import db

bp=Blueprint("jobs","jobs",url_prefix="/jobs")

@bp.route("/")
def alljobs():
    conn=db.get_db()
    cursor=con.cursor()
    cursor.execute("select o.id,o.title,o.

